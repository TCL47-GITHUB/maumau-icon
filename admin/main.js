// Danh sách icon mới
var fontName = "maumau-admin";

var prefix = "ad-mm ";

let abc = "adm";
// Danh sách icon mới
var newIcons = [
  `${abc}-share-link`,
  `${abc}-solid-share-link`,
  `${abc}-list-link`,
  `${abc}-solid-list-link`,
  `${abc}-save`,
  `${abc}-solid-save`,
  `${abc}-solid-star-half`,
  `${abc}-x`,
];
